const menuOpenButton=document.querySelector("#menu-open-button")
menuOpenButton.addEventListener("click"() => {
    //*toggle mobile menu visibility
    docucment.body.classlist.toggle("sghow-mobile-menu");
}